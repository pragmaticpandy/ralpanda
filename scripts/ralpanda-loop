#!/usr/bin/env bash
# ralpanda-loop — Entry point for the ralpanda autonomous agent loop.
# Delegates to the Python package in the same directory.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONPATH="${SCRIPT_DIR}:${PYTHONPATH:-}"
exec python3 -m ralpanda "$@"
